# 6. **Liters to Milliliters**
# Convert liquid quantity from L to mL.

# Input liquid quantity in liters
liters = float(input("\nEnter quantity in liters: "))

# Conversion
milliliters = liters * 1000

# Output
print("Quantity in milliliters:", round(milliliters, 2),"ml\n")